<body>
        <img id="home-page-img" src="/sunset.png">
    {{> navbar }}
    <section>
    <h1> These are all our supporters {{#if SignersByCity }} in {{city}} {{/if}}!</h1>
 
 <div id="form">

    <ul id="signers">{{#signers}} 
      <a href={{url}}><li>{{first}} {{last}}</a> {{age}} <a href="signers/{{city}}">{{city}}</li></a> </br>              
        {{/signers}}
        </ul> 
    </div>
    <div id="form"></div>
    <ul id="signers">
        {{#signersByCity}}
        <a href={{url}}><li>{{first}} {{last}}</a> {{age}} </br>   
        {{/signersByCity}} 
    </ul> 
    </div>
</section>
    
</body>
</html>

{{!-- //<a href="'/signers/'+{{city}}"></a> --}}